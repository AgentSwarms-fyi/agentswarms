node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
routeTree.gen.ts

# Generated by scripts/build-docs-index.mjs. Formatting it would change the
# bytes without changing the data, and the --check that keeps it in step with
# the pages compares exactly those bytes.
src/components/docs/docsIndex.ts

# The licence is canonical upstream text. Formatting it changes nothing legally
# but stops LICENSE.md diffing clean against the published Elastic License 2.0,
# which is the first thing anyone evaluating the licence does. `npm run format`
# rewrote its emphasis markers once; that is once too often.
LICENSE.md
